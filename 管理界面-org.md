## 指令：生成 Vue 3 + Element Plus 后台管理系统（含左侧栏、头部导航、操作栏）

请生成一个完整的、可直接在浏览器中运行的 **HTML 文件**，技术栈为 **Vue 3**（CDN，Composition API）、**Element Plus**（含图标库）和 **axios**。实现典型后台管理系统的布局与交互。

### 布局要求（三大区域）

1. **左侧功能栏（可折叠）**  
   - 垂直菜单，展示系统主要模块（如仪表盘、用户管理、订单列表、设置）。  
   - 每个菜单项带图标和文字。  
   - 支持折叠/展开：折叠时仅显示图标，宽度缩小；展开时显示图标+文字。  
   - 折叠状态需要在页面刷新后保持。  
   - 点击菜单项时，右侧主内容区域切换到对应的页面内容。
   - 选中的菜单项高亮显示，字体为纯白色。

2. **头部导航栏**  
   - 位于顶部，与左侧栏同一水平线，覆盖主区域上方。  
   - 左侧有折叠/展开左侧栏的切换按钮。  
   - 右侧包含：动态面包屑导航（根据当前激活菜单显示路径）、通知图标（带未读标记）、用户头像和下拉菜单（含“个人中心”、“退出登录”，仅模拟）。  
   - 整体样式与左侧栏有视觉区分（如不同背景色或阴影）。

3. **操作栏**  
   - 位于主内容区内部，头部导航栏下方。  
   - 包含常见操作组件：  
     - 左侧：新建、批量删除、筛选等按钮。  
     - 右侧：搜索输入框、日期范围选择器。  
   - 所有按钮点击时调用 `axios` 模拟请求（仅 `console.log` 或 `alert` 提示，无需真实后端）。  
   - 操作栏下方为动态内容卡片区，根据左侧菜单选择显示不同内容：
     - 仪表盘：展示几个统计卡片，数据通过 `axios` 获取（使用 mock 数据模拟异步请求）。  
     - 用户管理：展示用户表格（含姓名、邮箱、角色、状态），数据通过 `axios` 获取（mock 数据）。表格支持编辑/删除操作按钮（仅演示）。  
     - 其他菜单项可仅显示占位文本。

### 交互与状态要求

- 当前激活的菜单项需高亮显示。  
- 菜单切换时，操作栏的标题（如“仪表盘”→“用户管理”）和下方内容区同步更新。  
- 头部导航栏的面包屑随菜单切换而更新。  
- 折叠/展开侧边栏时，主内容区域应平滑调整宽度，无布局抖动。  
- 页面刷新后，恢复之前折叠状态和当前激活菜单项（使用本地存储）。  
- 响应式设计：当屏幕宽度小于 768px 时，左侧栏默认隐藏，通过头部按钮打开一个抽屉式侧边栏（菜单依然可用），主区域始终全宽。

### 技术约束

- 使用 Vue 3 的 Composition API（`setup` 语法）。  
- 必须使用 Element Plus 组件库和图标（通过 CDN 引入）。  
- 使用 `axios` 发起模拟数据请求（可自行构造 `Promise` 返回本地数据，请求路径可写为 `/api/...` 并拦截或直接使用 mock）。  
- 所有代码应放在一个 `.html` 文件中，包含 `<style>` 和 `<script>`，确保打开即运行，无构建步骤。  
- 不依赖任何额外的 UI 框架或插件。

### 视觉效果

- 使用 Element Plus 默认浅色主题，主区域背景为浅灰色（`#f5f7fa`）。  
- 卡片、表格等组件有圆角和适当阴影。  
- 菜单 hover 与 active 样式使用 Element Plus 默认样式。  
- 页面高度为 `100vh`，无横向滚动条。

### 验收标准

1. 页面加载后，左侧栏可见图标和文字，默认激活“仪表盘”，右侧显示统计卡片（带 axios 获取的 mock 数据）。  
2. 点击头部折叠按钮，左侧栏宽度缩小（仅图标），再次点击恢复。  
3. 点击“用户管理”菜单，右侧内容区变为用户表格（表格数据来自 axios mock），操作栏标题变为“用户管理”。  
4. 操作栏的按钮点击均有模拟请求提示。  
5. 浏览器宽度小于 768px 时，左侧栏隐藏，头部按钮打开抽屉式侧边栏。  
6. 刷新页面后，折叠状态和当前激活菜单保持不变。  

请生成完整代码，确保功能完整、无报错、交互流畅。