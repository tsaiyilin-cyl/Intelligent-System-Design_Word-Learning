# 单词管家 — Word Manager

一个基于 **Spring Boot 4 + JPA + MySQL** 的英语单词学习系统，支持智能复习、测试考核、学习报告等功能。

---

## 功能一览

- **单词库** — 内置小学初中高中阶段考纲词汇，支持自定义添加单词
- **智能复习** — 基于艾宾浩斯遗忘曲线的熟悉度衰减算法，自动安排复习
- **多种题型** — 英译中、中译英、拼写多种测试模式
- **错题本** — 自动收录错题，支持分类复习
- **学习报告** — 学习进度、掌握统计、连续打卡天数概览
- **图文结合** — 自动搜索配图辅助记忆
- **学习计划** — 自定义每日学习目标与阶段（小学/初中/高中）

---

## 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | Spring Boot 4 + Spring MVC |
| 数据层 | Spring Data JPA + Hibernate |
| 数据库 | MySQL 8.0 |
| 前端 | 原生 HTML + CSS + JavaScript |
| 构建工具 | Maven（含 Wrapper，无需本地安装） |
| Java 版本 | 17 |

---

## 项目结构

```
src/main/java/cn/edu/cuc/class10/
├── Class10Application.java      # 启动入口
├── WebConfig.java               # 静态资源配置
├── config/
│   └── RestTemplateConfig.java  # HTTP 客户端配置
├── controller/                  # REST API 控制器
├── service/                     # 业务逻辑层（含数据初始化器）
├── entity/                      # JPA 实体类
├── repository/                  # 数据访问层
├── dto/                         # 数据传输对象
└── resources/
    ├── application.yaml         # 配置文件
    ├── vocabulary.json          # 纲词汇数据
    └── static/                  # 前端 HTML 页面
```

---

## 数据库表结构

| 表名 | 说明 | 
|------|------|
| `users` | 用户账号与学习设置 | 
| `words` | 单词词典（含相似词、短语、例句） |
| `interactions` | 用户-单词互动记录 | 
| `user_word_familiarity` | 每个用户对每个单词的熟悉度评分 |
| `test_sessions` | 测试会话记录 | 
| `test_answer_records` | 每道题的答题详情 | 
| `mistake_words` | 错词本 | 

---

## 部署到其他机器（演示用）

目标机器只需安装 **JDK 17** 和 **MySQL 8.0**，无需 Maven 或 IDE。

### 文件

| 文件 | 大小 | 说明 |
|------|:----:|------|
| `class10.war` | 60 MB | 可执行程序（内嵌 Tomcat） |
| `class10_dump.sql` | 18 MB | 数据库完整备份（含建表 + 全部数据） |
| `word-images/` | ~900 MB | 单词配图缓存 |

### 部署步骤

**第 1 步 — 安装 JDK 17**

下载安装并配置环境变量：
- [Adoptium JDK 17](https://adoptium.net/temurin/releases/?version=17)
- 或 [Oracle JDK 17](https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html)

验证安装：
```bash
java -version
# 输出应为 openjdk 17.x.x 或 java 17.x.x
```

**第 2 步 — 安装 MySQL 8.0**

- [MySQL Community Server 8.0 下载](https://dev.mysql.com/downloads/mysql/)
- 安装时选择 MySQL Server，记好 root 密码

**第 3 步 — 导入数据库**

将 `class10_dump.sql` 拷贝到目标机器，打开命令行执行：

```bash
mysql -u root -p < class10_dump.sql
```

输入 MySQL 密码后，脚本会自动：
- 检查 `class10` 数据库是否存在，不存在则创建
- 重建 7 张表
- 插入全部数据

**第 4 步 — 启动项目**

```bash
java -jar class10.war
```

看到以下日志即启动成功：
```
Tomcat started on port 8080
Started Class10Application in X.XXX seconds
```

**第 5 步 — 访问**

浏览器打开 `http://localhost:8080`

---

## 开发环境启动

```bash
# 确保 MySQL 已运行，然后：
./mvnw spring-boot:run
```

访问 `http://localhost:8080`

> 默认数据库配置：`root:123456@localhost:3306/class10`，可在 `application.yaml` 中修改。

---

## 常见问题

**端口 8080 被占用？**
```bash
netstat -ano | findstr :8080
taskkill /F /PID <进程ID>
```
