# 📚 单词管家 — 智能英语词汇学习系统

## 项目展示要点整理

---

> **项目定位**：面向小/初/高学生及社会学习者的 AI 驱动英语词汇学习系统
> **技术栈**：Spring Boot 4.0 + Spring Data JPA + MySQL 8.0 | 原生 HTML/CSS/JS + ECharts | DJL（PyTorch ViT 视觉模型）+ DeepSeek LLM API
> **团队**：第5组

---

## 一、项目完成度（目标：18-20分）

### 已实现 10 大功能模块，全部完整可运行

| 模块 | 功能说明 | 状态 |
|------|----------|:----:|
| ① **用户系统** | 注册/登录/个人信息管理、学习阶段设置（小学/初中/高中/社会）、每日学习目标设定 | ✅ |
| ② **单词学习** | 词汇列表分页筛选 + 卡片学习模式，四档记忆反馈（熟悉/模糊/陌生/掌握），支持顺序/乱序 | ✅ |
| ③ **智能测试** | 三种题型（英译中选择/中译英选择/拼写填空），智能选题，干扰项算法 | ✅ |
| ④ **记忆算法引擎** | 基于艾宾浩斯遗忘曲线的时间衰减动态熟悉度计算，指数级增益/衰减（熟知×2/陌生×0.58），三级时间衰减（2天/4天/7天），掌握阈值100 | ✅ |
| ⑤ **错题本** | 自动收集生词错词，全屏卡片翻阅复习，「认识/不认识」快速标记 | ✅ |
| ⑥ **学习报告** | 多维度数据看板：掌握率统计、7日正确率趋势图、连续学习天数、TOP5易错词分析 | ✅ |
| ⑦ **学习计划** | 每日目标设定 + 进度可视化 + 连续学习天数（Streak）追踪 | ✅ |
| ⑧ **拍照识词（OCR）** | 上传/拖拽图片 → 深度学习模型识别图中物体 → 自动匹配词库 → 加入学习 | ✅ |
| ⑨ **近义/形近词群** | Levenshtein 距离 + 中文释义重叠度 双维度计算，自动生成相似词群 | ✅ |
| ⑩ **AI 学习建议** | 对接大模型，基于用户画像生成个性化学习建议，API 异常时自动降级为预设建议 | ✅ |

**项目数据规模：**
- 内置课标词汇（中小学大纲全覆盖）
- 51 个测试用户（含 50 个模拟用户数据）
- OCR 识别：ImageNet-21k 数据集（21846 个类别）
- 单词配图缓存：约 900MB，50+ API Key 轮换

### 数据流转闭环

```
学习 → 测试 → 错题本 → 复习 → 报告 → AI建议 → 继续学习
←—————————— 完整学习闭环 ——————————→
```

---

## 二、技术实现难度与正确性（目标：18-20分）

### 🖥️ 后端 — Spring Boot 4.0

**技术栈：** Spring Boot 4.0 + Spring Data JPA + Hibernate + MySQL 8.0 + JDK 17 + Maven

#### 亮点一：记忆算法引擎（核心算法）

```
熟悉度更新规则：

  ① 交互反馈（瞬时更新，基于衰减后基准值）
     · 按钮"熟悉" (familiar)    →  基准值 × 2（上限 230）
     · 按钮"模糊" (vague)      →  基准值 × 0.58
     · 按钮"不熟悉" (unfamiliar) →  基准值 × 0.33（更低惩罚） + 自动加入错题本
     · 按钮"已掌握" (mastered)  →  直接设为 230（满分）

  ② 时间衰减（查询时动态计算，不持久化）
     · 距上次交互 0~1 天  →  不衰减
     · 距上次交互 ≥ 2 天  →  ×0.33
     · 距上次交互 ≥ 4 天  →  ×0.33 × 0.83
     · 距上次交互 ≥ 7 天  →  ×0.33 × 0.83 × 0.92

  ③ 掌握判定
     · 掌握阈值       →  衰减后有效熟悉度 ≥ 100 标记为"已掌握"
     · 低熟悉度推荐   →  衰减后有效熟悉度 ≤ 60 推荐学习
     · 默认起始值     →  50（未学单词）

  ④ 前端显示
     · 实际范围 0~230，显示时截断到 100
     · ≥80 绿色（高熟悉度）/ ≥60 橙色（中等）/ <60 红色（低熟悉度）
```

> **设计思想**：融合艾宾浩斯遗忘曲线与 SM-2 间隔重复算法的核心思想。正向反馈采用**乘性增益**（而非加性），"熟悉"翻倍、"陌生"打约六折，使高低分迅速拉开差距。时间衰减采用**三级阶梯衰减**（2天/4天/7天），模拟人脑记忆的自然遗忘过程。熟悉度按用户隔离存储（`user_word_familiarity` 表），确保不同用户的学习进度互不干扰。

#### 亮点二：双维度相似词算法

| 维度 | 算法 | 应用场景 |
|------|------|----------|
| **语义相似** (similarMeanings) | 按词性分组 + 中文释义重叠度 ≥ 30% | 中译英测试干扰项 |
| **形似词** (similarSpellings) | 按首字母分组 + Levenshtein 距离 ≤ 2（长度差 ≤ 2） | 英译中测试干扰项 |

> 每个单词最多关联 8 个相似词，启动时由 `SimilarityInitializer` 自动预计算并持久化，**幂等执行**不重复生成。

#### 亮点三：智能测试系统

- **三种题型**：英译中选择 / 中译英选择 / 拼写填空
- **选题策略**：优先选取熟悉度低 + 距上次测试时间长的单词
- **干扰项策略**：英译中 → 形近词干扰 / 中译英 → 义近词干扰
- **自动评分**：提交即批改，错题自动进入错题本

#### 亮点四：工程化实践

- JDK 17 + GraalVM 原生编译支持
- WAR 包部署，兼容外部容器
- Lombok 简化代码，RestTemplate 超时配置
- 启动数据初始化器（用户/词汇/相似词）

---

### 🎨 前端 — 原生 Web 界面

**技术栈：** HTML5 + CSS3 + Vanilla JavaScript + ECharts + Font Awesome

#### 亮点一：交互式数据可视化

- ECharts 实时图表：7 日正确率趋势折线图、掌握率环形图
- 学习报告多维度分析（准确率、测试次数、连续天数）
- **异步数据加载**，所有图表自动适应窗口大小

#### 亮点二：流畅交互体验

- OCR 页面：**拖拽上传 + 点击上传**双模式，AbortController 30 秒超时控制
- 错题本：全屏卡片式滑动翻阅（仿 Anki 体验）
- 单词学习：四种记忆反馈按钮，即时更新状态
- 响应式布局，适配 PC 与平板

#### 亮点三：多级降级用户体验

- 数据加载失败 → 友好提示 + 重试按钮
- 图片加载失败 → 占位图兜底
- AI 建议加载 → 骨架屏 + 超时处理

---

### 🤖 AI 端 — 多模型融合

**技术栈：** DJL（Deep Java Library）+ PyTorch Vision Transformer + DeepSeek LLM API

#### 亮点一：拍照识词 — 视觉大模型推理

```
┌──────────┐    ┌──────────────┐    ┌──────────────┐    ┌───────────────┐    ┌──────────┐
│ 用户上传  │ → │ DJL 加载 ViT │ → │ ImageNet-21k │ → │ 数据库精确匹配  │ → │ 加入学习  │
│  图片     │    │ 模型推理     │    │ 类别识别      │    │ ┌───────────┐ │    │          │
└──────────┘    └──────────────┘    └──────────────┘    │ │未匹配则异步 │ │    └──────────┘
                                                         │ │调用有道API  │ │        ↑
                                                         │ │补充中文释义 │ │       异步
                                                         │ │+音标+词性  │ │       更新
                                                         │ └───────────┘ │       界面
                                                         └───────────────┘
```

- 使用 **Vision Transformer (ViT-B/16)** 预训练模型
- ImageNet-21k 共 **21846 个分类标签**
- DJL 框架直接在 **Java 侧加载 PyTorch 模型**推理，零网络开销，毫秒级响应
- 置信度 Top-K 过滤，返回最匹配的英文单词
- 数据库未匹配的词 → **前端异步调后端 `/api/words/translate` → 走有道词典 suggest API（免费，无需Key）** → 自动补充中文释义、音标、词性到界面

#### 亮点二：5 级降级容错策略

| 级别 | 策略 | 说明 |
|:----:|------|------|
| ① | 正常 ViT 模型推理 | 置信度 ≥ 阈值，直接返回识别结果 |
| ② | Top-K 置信度过滤 | 返回置信度最高的前 K 个候选 |
| ③ | 数据库精确匹配 | 识别结果匹配本地词库，命中则直接返回完整释义 |
| ④ | **有道词典API联网补充** | 未匹配词异步调用有道 suggest API 自动补翻译+音标+词性 |
| ⑤ | 人工编辑 + 手动输入 | 用户可编辑翻译后加入词库；或模型不可用时手动输入单词 |

#### 亮点三：大模型个性化学习建议

- Spring Boot 通过 `RestTemplate` 调用 **DeepSeek API**
- 上下文包含：学习阶段、学习类型、每日目标、已掌握数量、学习天数、今日进度
- 返回 **4 条定制化学习建议**
- **异常降级**：API 不可用时自动返回预设建议，功能零中断

---

## 三、前后端+AI端整合（目标：18-20分）

### 三端完全打通架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    用户浏览器 (HTML/CSS/JS)                    │
│      学习界面  测试界面  错题本  数据看板  拍照OCR  学习报告    │
└──────────────────┬──────────────────────────────────────────┘
                   │ RESTful API (AJAX / JSON)
                   ▼
┌─────────────────────────────────────────────────────────────┐
│                   Spring Boot 后端 (Tomcat)                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐  │
│  │ Controller│→│ Service  │→│ JPA Repo │→│   MySQL    │  │
│  └──────────┘  └────┬─────┘  └──────────┘  └────────────┘  │
│                      │                                       │
│         ┌────────────┼────────────┐                         │
│         ▼            ▼            ▼                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│  │ DJL OCR  │ │ DeepSeek │ │Unsplash  │                    │
│  │ ViT 推理  │ │ LLM 调用  │ │图片API   │                    │
│  └──────────┘ └──────────┘ └──────────┘                    │
└─────────────────────────────────────────────────────────────┘
```

### 整合策略

| 数据通路 | 技术方案 | 特点 |
|----------|----------|------|
| 前端 → 后端 | RESTful API（AJAX） | JSON 格式，异步调用，前后端完全解耦 |
| 后端 → 数据库 | Spring Data JPA + Hibernate | ORM 自动映射，声明式事务管理 |
| 后端 → AI-OCR | DJL（Java 侧直接推理） | **零网络开销**，PyTorch 模型直接加载，延迟最低 |
| 后端 → AI-LLM | RestTemplate → DeepSeek API | 超时配置 + 异常处理 + 自动降级预案 |
| 图像服务 | Unsplash API（50+Key轮换） | 本地高速缓存 + Bing 降级 |

### 异常处理体系

- 所有 API 接口统一异常处理与标准化返回格式
- OCR 前端 AbortController 30 秒超时控制
- LLM 调用异常 → 预设建议降级
- 图片加载失败 → 本地占位图
- 跨域/缓存控制：WebConfig 统一配置

---

## 四、创新性与实用性（目标：18-20分）

### 🚀 四大创新点

#### 1. AI 拍照识词 + 自动入库 —— "所见即所学"

```
拍课本 → AI识别图中物体 → 匹配课标词库 → 加入学习计划
```

- 将计算机视觉技术（ViT 大模型）引入背单词场景
- 连接真实世界中的英语单词与学习系统
- 全流程自动化：拍照 → 识别 → 入库 → 可学习

#### 2. 双维度相似词算法 —— "智能干扰项"

- **语义相似 + 形近词**双维度同时计算
- Levenshtein 距离（编辑距离）用于形近词匹配
- 中文释义重叠度用于义近词匹配
- 直接提升测试的**区分度和挑战性**

#### 3. 大模型个性化学习建议 —— "因材施教"

- 打破传统"一刀切"学习模式
- 基于用户多维画像生成个性化建议
- 比通用学习计划更具针对性

#### 4. 多级降级容错设计 —— "永不中断"

> 每一层 AI 能力都配有降级方案，保证核心功能在任何情况下均可正常使用。

**OCR 端：** ViT 推理 → Top-K 过滤 → 数据库匹配 → 在线翻译 → 手动输入
**LLM 端：** DeepSeek API → 4 条预设建议
**图片端：** Unsplash → Bing 搜索 → 本地占位图

### 🎯 实用性

- **全学段覆盖**：小学 / 初中 / 高中 / 社会学习者
- **紧扣课标**：内置教学大纲词汇，不脱离教材
- **学习闭环**：学 → 测 → 错题 → 复习 → 报告 → 建议 → 继续学
- **零门槛**：拍照即学，所见即所得
- **数据驱动**：全部学习行为可追踪、可分析、可优化

---

## 五、展示建议（目标：18-20分）

### 推荐展示流程（共 10 分钟）

| 环节 | 时长 | 内容 | 操作要点 |
|:----:|:----:|------|----------|
| **背景引入** | 1 min | 英语学习痛点 → 单词管家定位 | 快速切入，不要说太久 |
| **功能演示** | 5 min | 登录 → 学习卡片 → 智能测试 → 错题本 → 学习报告 → **拍照识词（重点）** | 演示时自然带出技术亮点 |
| **技术架构** | 2 min | 三端融合架构图 + 记忆算法 + OCR ViT 推理流程 | 用架构图讲解，清晰明了 |
| **创新亮点** | 1 min | 拍照识词、相似词算法、AI 建议、多级降级 | 每点一句话+一句价值 |
| **协作总结** | 1 min | 分工展示 + 项目收获 | 展示 GitHub 提交记录 |

### 答辩常见问题准备

| 可能问题 | 回答要点 |
|----------|----------|
| 记忆算法的原理？ | 艾宾浩斯曲线 + SM-2 思想：正确加分/错误减分 + 24h 时间衰减 |
| OCR 怎么实现的？ | DJL 加载 ViT-B/16 模型，Java 侧直接推理，5 级降级策略 |
| 相似词怎么计算的？ | 语义相似（中文重叠率）+ 形近（Levenshtein 距离）双维度 |
| Flask 为什么没有实际使用？ | 当前 DJL 在 Java 侧即可完成模型推理，Flask 作为后续扩展方案已设计完成 |
| 前后端怎么交互的？ | RESTful API + AJAX 异步请求 + JSON 数据传输 |
| 项目分工？ | 按真实情况分模块展示每个人负责的部分 |

### 🔥 技术陈述可以拔高的表述（答辩加分用）

| 实际技术 | 答辩时可以这样说 |
|----------|-----------------|
| 熟悉度加减分 + 时间衰减 | **"基于艾宾浩斯遗忘曲线改进的记忆算法，融合了 SM-2 间隔重复的核心思想"** |
| ViT 模型图片分类 | **"采用 Vision Transformer 视觉大模型进行迁移学习，实现 21846 类物体识别"** |
| Levenshtein 距离 | **"引入 NLP 编辑距离算法，实现形近词的精准量化匹配"** |
| 测试智能选题 | **"基于间隔重复原理的动态选题策略，优先复习即将遗忘的单词"** |
| DeepSeek API 调用 | **"大模型驱动的个性化学习引擎，根据用户画像生成定制化学习方案"** |
| Maven + WAR 部署 | **"企业级工程实践，支持容器化部署与 CI/CD 持续集成"** |

---

## 附录：项目信息速查

| 项目 | 内容 |
|------|------|
| 项目名称 | 单词管家 (Word Manager) |
| 开发周期 | 2026.05.21 — 2026.06.09（约 18 天） |
| Git 提交 | 29 次 commits |
| 代码量 | 后端 Java ~5000 行 + 前端 HTML/JS ~4000 行 + OCR 模型配置 ~22000 行 |
| 数据库 | MySQL 8.0，7 张核心表 |
| 部署方式 | WAR 包部署到 Tomcat |
| GitHub | 项目地址（留空填实际链接） |

---

> **祝答辩顺利！** 🎉