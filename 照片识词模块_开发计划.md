# 照片识词模块 — 开发计划

## 1. 项目概述

**目标**：实现"照片识词"功能——用户上传一张表示物体的图片（如青蛙的照片），系统自动识别图中的物体并推荐对应的英文单词，一键加入词汇库。

**负责人**：蔡奕麟

**技术栈**：
- 后端：Spring Boot 4 + DJL (Deep Java Library) + ONNX Runtime
- 模型：ResNet-152 预训练于 ImageNet-21k（~21841 类）
- 前端：Vue 3 + Element Plus（集成在 dashboard.html 的已有 photo_ocr 视图中）
- 数据源：现有 words 表（考纲词汇 + 自建词汇）

---

## 2. 整体流程

```
用户上传图片 (POST /api/ocr/recognize, multipart)
    │
    ▼
DJL + ONNX Runtime 加载图片 → 模型推理 → top-3 分类结果
    │  (如 "tree_frog", "bullfrog", "green_frog")
    ▼
在 words 表中模糊搜索匹配的英文单词
    │  如果找到 → 返回单词详情（释义、音标、词性、熟悉度）
    │  没找到   → 将分类标签名称作为候选单词返回
    ▼
前端展示候选单词列表（带置信度）
    │
    ▼
用户确认一个候选词，点击"加入词库"
    │
    ▼
POST /api/ocr/add-word → 以 CUSTOM 类型写入 words 表
    │
    ▼
完成，返回首页
```

---

## 3. 依赖修改

### pom.xml — 新增依赖

```xml
<!-- DJL 核心 -->
<dependency>
    <groupId>ai.djl</groupId>
    <artifactId>api</artifactId>
    <version>0.31.0</version>
</dependency>

<!-- ONNX Runtime 引擎 -->
<dependency>
    <groupId>ai.djl.onnxruntime</groupId>
    <artifactId>onnxruntime-engine</artifactId>
    <version>0.31.0</version>
</dependency>

<!-- DJL 模型仓库（方便本地模型下载） -->
<dependency>
    <groupId>ai.djl</groupId>
    <artifactId>model-zoo</artifactId>
    <version>0.31.0</version>
</dependency>
```

### application.yaml — 新增配置

```yaml
# ===== OCR 图片识别配置 =====
ocr:
  # 模型下载目录（相对于项目根目录）
  model-directory: ocr-models
  # 模型文件 URL（ImageNet-21k 预训练 ONNX 模型）
  model-url: "https://example.com/resnet152_21k.onnx"
  # 分类标签文件
  labels-file: "synset_21k.txt"
  # 推理时返回 top-K 个候选
  top-k: 3
  # 最低置信度阈值（低于此值不返回）
  confidence-threshold: 0.3
```

---

## 4. 后端新增文件

### 4.1 OcrService.java — 核心图像分类服务

**路径**：`src/main/java/cn/edu/cuc/class10/service/OcrService.java`

**职责**：
1. 应用启动时自动下载/加载 ImageNet-21k ONNX 模型（首次运行下载，后续缓存）
2. 加载 synset_21k.txt 分类标签文件（~21841 行，每行一个英文标签）
3. 提供 `recognize(MultipartFile image)` 方法：
   - 读取图片 → 预处理（resize 到 224×224，归一化）
   - ONNX Runtime 推理 → 获取分类概率向量
   - Softmax → 取 top-3 结果（类别索引 + 置信度）
   - 映射为英文标签名
4. 返回 `List<RecognitionResult>`（每个结果包含 label、confidence）

**关键代码结构**：

```java
@Service
public class OcrService {
    
    @Value("${ocr.model-directory}")
    private String modelDirectory;
    
    @Value("${ocr.model-url}")
    private String modelUrl;
    
    @Value("${ocr.labels-file}")
    private String labelsFile;
    
    @Value("${ocr.top-k:3}")
    private int topK;
    
    @Value("${ocr.confidence-threshold:0.3}")
    private float confidenceThreshold;
    
    private Predictor<Image, float[]> predictor;
    private List<String> synsetLabels;  // ImageNet-21k 标签列表
    private boolean modelReady = false;
    
    @PostConstruct
    public void init() {
        // 1. 确保模型目录存在
        // 2. 加载 synset 标签文件（从 classpath 或外部路径）
        // 3. 检查模型文件是否存在，不存在则从 URL 下载
        // 4. 使用 DJL Criteria API 加载 ONNX 模型
        // 5. 创建 Predictor
    }
    
    public List<RecognitionResult> recognize(InputStream imageStream) {
        // 1. ImageFactory 读取图片
        // 2. 预处理（resize, normalize）
        // 3. predictor.predict()
        // 4. Softmax → topK 筛选
        // 5. 映射标签名
        // 6. 返回结果
    }
    
    public boolean isModelReady() {
        return modelReady;
    }
    
    // RecognitionResult 内部类
    @Data
    public static class RecognitionResult {
        private String label;       // 英文标签名
        private float confidence;   // 置信度（0~1）
        private String displayName; // 格式化后的单词（转小写、去下划线）
    }
}
```

### 4.2 OcrController.java — 照片识词 REST 接口

**路径**：`src/main/java/cn/edu/cuc/class10/controller/OcrController.java`

**接口列表**：

| 方法 | 路径 | 参数 | 返回 | 说明 |
|------|------|------|------|------|
| POST | `/api/ocr/recognize` | `file` (Multipart), `userId` | `List<Map>` | 上传图片，返回识别的候选单词列表，含数据库匹配结果 |
| POST | `/api/ocr/add-word` | `wordContent`, `translation`, `userId` | `Map` | 用户确认后将候选词加入词库（CUSTOM 类型） |
| GET | `/api/ocr/status` | 无 | `Map` | 查询模型加载状态（是否就绪） |

**`/api/ocr/recognize` 返回格式示例**：

```json
{
  "code": 200,
  "data": [
    {
      "label": "tree frog",
      "confidence": 0.85,
      "matched": true,
      "wordId": "xxx-xxx",
      "content": "frog",
      "translation": "青蛙",
      "phonetic": "/frɒɡ/",
      "partOfSpeech": "NOUN"
    },
    {
      "label": "bullfrog",
      "confidence": 0.08,
      "matched": false,
      "content": null,
      "translation": null
    }
  ]
}
```

---

## 5. 前端修改

### 5.1 dashboard.html — photo_ocr 视图改造

**位置**：在已有的 `<template v-if="currentView === 'photo_ocr'">` 中修改

**改动内容**：
1. 移除模拟识别的 `setTimeout` 假逻辑
2. 调用真实 API：`uploadPhoto()` → `POST /api/ocr/recognize`
3. 展示候选词卡片列表：
   - 每个候选词显示：置信度进度条 + 英文单词 + 中文释义（如有）
   - 数据库匹配到的词显示绿色"已收录"标记
   - 未匹配到的词显示"新增"标记
4. 添加"加入词库"按钮（点击后调用 `/api/ocr/add-word`）
5. 添加手动编辑输入框（用户可修改识别结果）
6. 添加识别中的 loading 状态

```html
<!-- 候选结果展示区 -->
<div v-if="ocrCandidates.length > 0" style="margin-top: 20px;">
  <h4 style="margin-bottom: 12px;">🎯 识别结果（选择要加入词库的单词）</h4>
  <div v-for="(cand, idx) in ocrCandidates" :key="idx"
       class="candidate-card"
       :class="{ selected: selectedCandidate === idx }"
       @click="selectedCandidate = idx">
    <div class="cand-header">
      <span class="cand-label">{{ cand.displayName }}</span>
      <el-tag v-if="cand.matched" type="success" size="small">已收录</el-tag>
      <el-tag v-else type="warning" size="small">新词</el-tag>
    </div>
    <div v-if="cand.matched" class="cand-trans">
      {{ cand.translation }}
    </div>
    <div class="cand-confidence">
      <el-progress :percentage="Math.round(cand.confidence * 100)" :stroke-width="6" />
    </div>
    <!-- 手动编辑 -->
    <div v-if="selectedCandidate === idx && !cand.matched" style="margin-top: 8px;">
      <el-input v-model="ocrEditWord" placeholder="编辑单词" size="small" />
    </div>
  </div>
  <div style="margin-top: 16px;">
    <el-button type="primary" @click="addOcrWordToLibrary" :disabled="selectedCandidate === null">
      ✅ 加入词库
    </el-button>
  </div>
</div>
```

---

## 6. 资源文件

### 6.1 synset_21k.txt — ImageNet-21k 分类标签

- 来源：ImageNet-21k 项目（官方标签映射）
- 格式：每行一个英文标签名，共 ~21841 行
- 位置：`src/main/resources/synset_21k.txt`
- 示例内容：
  ```
  n01443537 goldfish
  n01484850 great white shark
  n01491361 tiger shark
  ...
  ```

### 6.2 模型下载脚本（可选）

- 首次启动时自动从预置 URL 下载 ONNX 模型
- 下载到 `ocr-models/resnet152_21k.onnx`
- 后续启动检查文件存在即跳过下载

---

## 7. 实现步骤（按顺序）

| 步骤 | 内容 | 涉及文件 | 预计时间 |
|------|------|----------|----------|
| 1 | 添加 DJL + ONNX Runtime Maven 依赖 | `pom.xml` | 5 min |
| 2 | 下载 synset_21k.txt 标签文件 | `src/main/resources/synset_21k.txt` | 10 min |
| 3 | 实现 OcrService（模型加载、图片预处理、推理） | `OcrService.java` | 60 min |
| 4 | 实现 OcrController（识别、加入词库接口） | `OcrController.java` | 30 min |
| 5 | 修改 application.yaml 添加 OCR 配置 | `application.yaml` | 5 min |
| 6 | 修改 dashboard.html 的 photo_ocr 视图 | `dashboard.html` | 60 min |
| 7 | 修改 photo_ocr.html 独立页面（可选） | `photo_ocr.html` | 30 min |
| 8 | 端到端测试（上传→识别→匹配→加入词库） | — | 30 min |
| 9 | 模型下载 + 错误处理优化 | `OcrService.java` | 20 min |

---

## 8. 注意事项

### 8.1 模型文件大小
- ResNet-152 ONNX 模型约 350MB
- 首次启动需下载（下载时间取决于网络，约 5-15 分钟）
- 下载后缓存到 `ocr-models/` 目录，后续启动秒加载
- 需要在 `WebConfig.java` 中添加 `ocr-models/` 的静态资源映射（仅本地调试需要）

### 8.2 图片预处理要求
- 模型输入：224×224 RGB 图像
- 归一化：mean = [0.485, 0.456, 0.406], std = [0.229, 0.224, 0.225]
- DJL 的 ImageFactory 自带 resize 功能

### 8.3 数据库匹配逻辑
- 将分类标签名（如 "tree frog"）分词，分别搜索 words 表
- 优先匹配 content 字段完全匹配
- 其次匹配模糊搜索（LIKE %keyword%）
- 都找不到则作为新词候选

### 8.4 模型不可用时的降级策略
- 模型正在下载中 → 前端显示"模型加载中，请稍后再试"
- 模型下载失败 → 提示用户检查网络，或手动下载模型文件放到指定目录
- 模型推理失败 → 允许用户手动输入单词名
